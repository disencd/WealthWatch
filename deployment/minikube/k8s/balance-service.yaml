apiVersion: v1
kind: ConfigMap
metadata:
  name: balance-service-config
  namespace: splitwise
data:
  ENVIRONMENT: "development"
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_USER: "splitwise"
  DB_NAME: "splitwise"
  DB_SSLMODE: "disable"
  REDIS_HOST: "redis-service"
  REDIS_PORT: "6379"
  BALANCE_SERVICE_PORT: "8004"
  LOG_LEVEL: "info"
  AUTH_SERVICE_HOST: "auth-service"
  AUTH_SERVICE_PORT: "8001"

---
apiVersion: v1
kind: Secret
metadata:
  name: balance-service-secret
  namespace: splitwise
type: Opaque
data:
  DB_PASSWORD: c3BsaXR3aXNlcGFzc3dvcmQ=  # base64: splitwisepassword

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: balance-service
  namespace: splitwise
  labels:
    app: balance-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: balance-service
  template:
    metadata:
      labels:
        app: balance-service
    spec:
      containers:
      - name: balance-service
        image: splitwise/balance-service:latest
        ports:
        - containerPort: 8004
        envFrom:
        - configMapRef:
            name: balance-service-config
        - secretRef:
            name: balance-service-secret
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8004
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8004
          initialDelaySeconds: 5
          periodSeconds: 5
        volumeMounts:
        - name: tmp
          mountPath: /tmp
      volumes:
      - name: tmp
        emptyDir: {}

---
apiVersion: v1
kind: Service
metadata:
  name: balance-service
  namespace: splitwise
spec:
  selector:
    app: balance-service
  ports:
  - port: 8004
    targetPort: 8004
  type: ClusterIP
